"""
Staff models.

This module doesn't define any models currently, as staff information
is stored in the User and Profile models. This file exists to satisfy
Django's app requirements.

Position-based permissions are defined in permissions.py.
"""
from django.db import models

# No models needed currently - staff data is in users.Profile
